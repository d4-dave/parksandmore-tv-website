export function ResortStatusBar() {
  // Park hours/open-closed state is intentionally not inferred from attraction waits.
  // Render this rail only after an approved public park-hours/status contract is assigned.
  return null;
}
